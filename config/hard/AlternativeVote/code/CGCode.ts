const C: number = parseInt(readline());
for (let i = 0; i < C; i++) {
    const name: string = readline();
}
const V: number = parseInt(readline());
for (let i = 0; i < V; i++) {
    const votes: string = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');