<?php
fscanf(STDIN, "%d %d", $width, $height);
for ($i = 0; $i < $width; $i++)
{
    $blackGroups = stream_get_line(STDIN, 256 + 1, "\n");
}
for ($i = 0; $i < $height; $i++)
{
    $blackGroups = stream_get_line(STDIN, 256 + 1, "\n");
}
for ($i = 0; $i < $width; $i++)
{

    // Write an answer using echo(). DON'T FORGET THE TRAILING \n
    // To debug: error_log(var_export($var, true)); (equivalent to var_dump)

    echo("WhiteGroups\n");
    echo("loop height write WhiteGroups\n");
}