var inputs = readline().split(' ');
const N = parseInt(inputs[0]);
const K = parseInt(inputs[1]);
for (let i = 0; i < K; i++) {
    var inputs = readline().split(' ');
    for (let j = 0; j < N; j++) {
        const heap = parseInt(inputs[j]);
    }
}
for (let i = 0; i < K; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('1:4');
}