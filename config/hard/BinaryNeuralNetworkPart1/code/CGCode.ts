var inputs: string[] = readline().split(' ');
const inputs: number = parseInt(inputs[0]);
const outputs: number = parseInt(inputs[1]);
const hiddenLayers: number = parseInt(inputs[2]);
const testInputs: number = parseInt(inputs[3]);
const trainingExamples: number = parseInt(inputs[4]);
const trainingIterations: number = parseInt(inputs[5]);
var inputs: string[] = readline().split(' ');
for (let i = 0; i < hiddenLayers; i++) {
    const nodes: number = parseInt(inputs[i]);
}
for (let i = 0; i < testInputs; i++) {
    const testInput: string = readline();
}
for (let i = 0; i < trainingExamples; i++) {
    var inputs: string[] = readline().split(' ');
    const trainingInputs: string = inputs[0];
    const expectedOutputs: string = inputs[1];
}
for (let i = 0; i < testInputs; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}