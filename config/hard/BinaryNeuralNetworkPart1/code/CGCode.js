var inputs = readline().split(' ');
const inputs = parseInt(inputs[0]);
const outputs = parseInt(inputs[1]);
const hiddenLayers = parseInt(inputs[2]);
const testInputs = parseInt(inputs[3]);
const trainingExamples = parseInt(inputs[4]);
const trainingIterations = parseInt(inputs[5]);
var inputs = readline().split(' ');
for (let i = 0; i < hiddenLayers; i++) {
    const nodes = parseInt(inputs[i]);
}
for (let i = 0; i < testInputs; i++) {
    const testInput = readline();
}
for (let i = 0; i < trainingExamples; i++) {
    var inputs = readline().split(' ');
    const trainingInputs = inputs[0];
    const expectedOutputs = inputs[1];
}
for (let i = 0; i < testInputs; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}