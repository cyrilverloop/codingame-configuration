var inputs: string[] = readline().split(' ');
const N: number = parseInt(inputs[0]);
const M: number = parseInt(inputs[1]);
for (let i = 0; i < M; i++) {
    var inputs: string[] = readline().split(' ');
    const house1: number = parseInt(inputs[0]);
    const house2: number = parseInt(inputs[1]);
    const cost: number = parseInt(inputs[2]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('number of connections and total cost');
console.log('k lines: house1 house2');