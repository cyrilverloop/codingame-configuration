const SIZE = parseInt(readline());
for (let i = 0; i < SIZE; i++) {
    const ROW = readline();
}
for (let i = 0; i < SIZE; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('revealed row');
}