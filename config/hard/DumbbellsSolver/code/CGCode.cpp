#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int n;
    cin >> n; cin.ignore();
    int h;
    int w;
    cin >> h >> w; cin.ignore();
    for (int i = 0; i < h; i++) {
        string line;
        getline(cin, line);
    }
    for (int i = 0; i < h; i++) {

        // Write an answer using cout. DON'T FORGET THE "<< endl"
        // To debug: cerr << "Debug messages..." << endl;

        cout << "o=o" << endl;
    }
}