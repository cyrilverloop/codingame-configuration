var inputs = readline().split(' ');
const xs = parseInt(inputs[0]);
const ys = parseInt(inputs[1]);
var inputs = readline().split(' ');
const xd = parseInt(inputs[0]);
const yd = parseInt(inputs[1]);
const N = parseInt(readline());
for (let i = 0; i < N; i++) {
    var inputs = readline().split(' ');
    const xi = parseInt(inputs[0]);
    const yi = parseInt(inputs[1]);
    const wi = parseInt(inputs[2]);
    const hi = parseInt(inputs[3]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('distance');