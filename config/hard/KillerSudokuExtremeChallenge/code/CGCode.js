const numPuzzles = parseInt(readline());
for (let i = 0; i < numPuzzles; i++) {
    const cageLayout = readline();
}
for (let i = 0; i < numPuzzles; i++) {
    const cageValues = readline();
}
for (let i = 0; i < 9; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('9 space separated integers from row of solution matrix');
}