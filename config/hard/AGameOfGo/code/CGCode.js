const S = parseInt(readline()); // the size of the board
const M = parseInt(readline()); // the number of moves to be made
for (let i = 0; i < S; i++) {
    const row = readline(); // a single row in the input board
}
for (let i = 0; i < M; i++) {
    const move = readline(); // a strng representation of a move
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('NOT_VALID | <<the_board_after_the_moves>>');