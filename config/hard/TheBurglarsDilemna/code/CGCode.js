const N = parseInt(readline());
const C = parseInt(readline());
for (let i = 0; i < N; i++) {
    const numbers = readline();
}
for (let i = 0; i < N; i++) {
    const clicks = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('0 0 0 0');