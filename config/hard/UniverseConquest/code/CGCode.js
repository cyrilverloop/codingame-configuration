var inputs = readline().split(' ');
const planets = parseInt(inputs[0]);
const hyperspaceRoads = parseInt(inputs[1]);
for (let i = 0; i < planets; i++) {
    var inputs = readline().split(' ');
    const faction = inputs[0];
    const ships = parseInt(inputs[1]);
}
for (let i = 0; i < hyperspaceRoads; i++) {
    var inputs = readline().split(' ');
    const planet1 = parseInt(inputs[0]);
    const planet2 = parseInt(inputs[1]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');