var inputs: string[] = readline().split(' ');
const vampireCount: number = parseInt(inputs[0]);
const zombieCount: number = parseInt(inputs[1]);
const ghostCount: number = parseInt(inputs[2]);
const size: number = parseInt(readline());
var inputs: string[] = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromTop: number = parseInt(inputs[i]);
}
var inputs: string[] = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromBottom: number = parseInt(inputs[i]);
}
var inputs: string[] = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromLeft: number = parseInt(inputs[i]);
}
var inputs: string[] = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromRight: number = parseInt(inputs[i]);
}
for (let i = 0; i < size; i++) {
    const row: string = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');