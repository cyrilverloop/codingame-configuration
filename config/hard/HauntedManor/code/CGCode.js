var inputs = readline().split(' ');
const vampireCount = parseInt(inputs[0]);
const zombieCount = parseInt(inputs[1]);
const ghostCount = parseInt(inputs[2]);
const size = parseInt(readline());
var inputs = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromTop = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromBottom = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromLeft = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < size; i++) {
    const canSeeFromRight = parseInt(inputs[i]);
}
for (let i = 0; i < size; i++) {
    const row = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');