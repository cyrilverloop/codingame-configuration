var inputs = readline().split(' ');
const unsedP = inputs[0];
const unsedCnf = inputs[1];
const V = parseInt(inputs[2]);
const C = parseInt(inputs[3]);
for (let i = 0; i < C; i++) {
    const clause = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('s UNSATISFIABLE');