var inputs = readline().split(' ');
const h = parseInt(inputs[0]);
const w = parseInt(inputs[1]);
const t = parseFloat(readline());
const d = parseFloat(readline());
const n = parseInt(readline());
for (let i = 0; i < n; i++) {
    var inputs = readline().split(' ');
    const id = parseInt(inputs[0]);
    const x = parseFloat(inputs[1]);
    const y = parseFloat(inputs[2]);
    const vx = parseFloat(inputs[3]);
    const vy = parseFloat(inputs[4]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('{id} [{x},{y}]');