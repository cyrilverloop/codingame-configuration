const N = parseInt(readline());
for (let i = 0; i < N; i++) {
    var inputs = readline().split(' ');
    const SHIP = inputs[0];
    const HP = parseInt(inputs[1]);
    const ARMOR = parseInt(inputs[2]);
    const DAMAGE = parseInt(inputs[3]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('PEWPEW');