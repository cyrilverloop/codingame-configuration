var inputs = readline().split(' ');
const L = parseInt(inputs[0]);
const W = parseInt(inputs[1]);
const H = parseInt(inputs[2]);
for (let i = 0; i < L; i++) {
    var inputs = readline().split(' ');
    for (let j = 0; j < H; j++) {
        const row = inputs[j];
    }
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('activity density');