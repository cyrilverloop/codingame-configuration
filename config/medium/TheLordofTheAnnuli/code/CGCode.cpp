#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int width;
    int height;
    float cx;
    float cy;
    float ro;
    float ri;
    int samples;
    cin >> width >> height >> cx >> cy >> ro >> ri >> samples; cin.ignore();

    // Write an answer using cout. DON'T FORGET THE "<< endl"
    // To debug: cerr << "Debug messages..." << endl;

    cout << "ASCII art, including frame" << endl;
}