const r = parseInt(readline());
const b = parseInt(readline());
for (let i = 0; i < r; i++) {
    for (let j = 0; j < 3; j++) {
        const row = readline();
    }
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('row col');
console.log('number_of_rotations');