const n = parseInt(readline());
var inputs = readline().split(' ');
for (let i = 0; i < n; i++) {
    const rightSide = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < 9; i++) {
    const operandOccurrenceCount = parseInt(inputs[i]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('solutionCount (plus equations if solutionCount = 0)');