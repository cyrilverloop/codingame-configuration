<?php
$goaltile = stream_get_line(STDIN, 3 + 1, "\n");
fscanf(STDIN, "%d", $nrow);
for ($i = 0; $i < $nrow; $i++)
{
    $row = stream_get_line(STDIN, 42 + 1, "\n");
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n
// To debug: error_log(var_export($var, true)); (equivalent to var_dump)

echo("PUT tile rowid\n");
echo("rowid tile tile tile\n");