const w = parseInt(readline()); // width of ASCII-art picture (one meter per column)
const h = parseInt(readline()); // height of ASCII-art picture (one meter per line)
const B = parseFloat(readline()); // strengh of magnetic field (tesla)
const V = parseFloat(readline()); // speed of the particle (speed-of-light unit)
for (let i = 0; i < h; i++) {
    const line = readline(); // lines of ASCII-art picture
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');


// "<symbol> <radius>" if charged particle
// "<symbol> inf" if neutral particle
// "I just won the Nobel prize in physics !" if unknown particle
console.log('symbol radius');