for (let i = 0; i < 5; i++) {
    const keyTable = readline();
}
const action = readline();
const N = parseInt(readline());
for (let i = 0; i < N; i++) {
    const message = readline();
}
for (let i = 0; i < N; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}