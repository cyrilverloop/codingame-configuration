const H = parseInt(readline());
const W = parseInt(readline());
const S = parseInt(readline());
for (let i = 0; i < H; i++) {
    const line = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('Shape vertex X Y');