var inputs = readline().split(' ');
const w = parseInt(inputs[0]);
const h = parseInt(inputs[1]);
for (let i = 0; i < h; i++) {
    const line = readline();
}
const n = parseInt(readline());
for (let i = 0; i < n; i++) {
    const caseline = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');