const ciphertext = readline();
const N = parseInt(readline());
for (let i = 0; i < N; i++) {
    const word = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('plaintext');
for (let i = 0; i < 26; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log(''Na' or '{LETTER}' -> '{LETTER}'');
}