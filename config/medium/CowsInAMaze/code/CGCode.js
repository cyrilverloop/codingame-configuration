var inputs = readline().split(' ');
const c = parseInt(inputs[0]);
const n = parseInt(inputs[1]);
const m = parseInt(inputs[2]);
for (let i = 0; i < c; i++) {
    const cow = parseInt(readline());
}
for (let i = 0; i < n; i++) {
    var inputs = readline().split(' ');
    for (let j = 0; j < m; j++) {
        const square = parseInt(inputs[j]);
    }
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');