var inputs: string[] = readline().split(' ');
const c: number = parseInt(inputs[0]);
const n: number = parseInt(inputs[1]);
const m: number = parseInt(inputs[2]);
for (let i = 0; i < c; i++) {
    const cow: number = parseInt(readline());
}
for (let i = 0; i < n; i++) {
    var inputs: string[] = readline().split(' ');
    for (let j = 0; j < m; j++) {
        const square: number = parseInt(inputs[j]);
    }
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');