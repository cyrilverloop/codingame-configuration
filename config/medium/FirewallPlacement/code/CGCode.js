const numNodes = parseInt(readline());
const virusLocation = parseInt(readline());
const numLinks = parseInt(readline());
for (let i = 0; i < numLinks; i++) {
    const link = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');