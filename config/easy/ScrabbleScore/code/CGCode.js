const nbTiles = parseInt(readline()); // Number of tiles in the tile set
for (let i = 0; i < nbTiles; i++) {
    var inputs = readline().split(' ');
    const character = inputs[0];
    const score = parseInt(inputs[1]);
}
var inputs = readline().split(' ');
const width = parseInt(inputs[0]);
const height = parseInt(inputs[1]);
for (let i = 0; i < height; i++) {
    const emptyBoardRow = readline(); // Empty board with special cells
}
for (let i = 0; i < height; i++) {
    const previousWordBoardRow = readline(); // Words already played
}
for (let i = 0; i < height; i++) {
    const playedWordBoardRow = readline(); // Words after you play
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('OF 5');
console.log('Total 5');