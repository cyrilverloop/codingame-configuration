const total = parseInt(readline());
for (let i = 0; i < total; i++) {
    const symbol = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('combination(s)');