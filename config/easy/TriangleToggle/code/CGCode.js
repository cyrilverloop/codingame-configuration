var inputs = readline().split(' ');
const HI = parseInt(inputs[0]);
const WI = parseInt(inputs[1]);
const style = readline();
const howManyTriangles = parseInt(readline());
for (let i = 0; i < howManyTriangles; i++) {
    var inputs = readline().split(' ');
    const x1 = parseInt(inputs[0]);
    const y1 = parseInt(inputs[1]);
    const x2 = parseInt(inputs[2]);
    const y2 = parseInt(inputs[3]);
    const x3 = parseInt(inputs[4]);
    const y3 = parseInt(inputs[5]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('rectangleWithCarvings');