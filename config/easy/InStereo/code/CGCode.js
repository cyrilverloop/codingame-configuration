const P = parseInt(readline());
const pattern = readline();
const S = parseInt(readline());
const stock = readline();
var inputs = readline().split(' ');
const H = parseInt(inputs[0]);
const W = parseInt(inputs[1]);
for (let i = 0; i < H; i++) {
    const depthmap = readline();
}
for (let i = 0; i < H; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('answer');
}