const worktime = parseInt(readline());
const nc = parseInt(readline());
var inputs = readline().split(' ');
for (let i = 0; i < nc; i++) {
    const efficiency = parseFloat(inputs[i]);
}
const nv = parseInt(readline());
var inputs = readline().split(' ');
for (let i = 0; i < nv; i++) {
    const helptime = parseInt(inputs[i]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('answer');