var inputs = readline().split(' ');
const m = parseInt(inputs[0]);
const n = parseInt(inputs[1]);
const p = parseInt(inputs[2]);
var inputs = readline().split(' ');
const countA = parseInt(inputs[0]);
const countB = parseInt(inputs[1]);
for (let i = 0; i < countA; i++) {
    var inputs = readline().split(' ');
    const row = parseInt(inputs[0]);
    const column = parseInt(inputs[1]);
    const value = parseFloat(inputs[2]);
}
for (let i = 0; i < countB; i++) {
    var inputs = readline().split(' ');
    const row = parseInt(inputs[0]);
    const column = parseInt(inputs[1]);
    const value = parseFloat(inputs[2]);
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('row column value');