const nbTests = parseInt(readline());
for (let i = 0; i < nbTests; i++) {
    const deal = readline();
}
for (let i = 0; i < nbTests; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('0');
}