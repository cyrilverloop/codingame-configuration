const letterCase = readline();
const letterFuzz = parseInt(readline());
const numberFuzz = parseInt(readline());
const otherFuzz = readline();
const template = readline();
const n = parseInt(readline());
for (let i = 0; i < n; i++) {
    const candidate = readline();
}
for (let i = 0; i < n; i++) {

    // Write an answer using console.log()
    // To debug: console.error('Debug messages...');

    console.log('true/false');
}