var inputs = readline().split(' ');
const wallyWidth = parseInt(inputs[0]);
const wallyHeight = parseInt(inputs[1]);
for (let i = 0; i < wallyHeight; i++) {
    const wallyRow = readline();
}
var inputs = readline().split(' ');
const pictureWidth = parseInt(inputs[0]);
const pictureHeight = parseInt(inputs[1]);
for (let i = 0; i < pictureHeight; i++) {
    const pictureRow = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('x y');