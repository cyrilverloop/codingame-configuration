#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    string pieces;
    getline(cin, pieces);
    for (int i = 0; i < 8; i++) {
        string sline;
        getline(cin, sline);
    }
    for (int i = 0; i < 8; i++) {
        string eline;
        getline(cin, eline);
    }

    // Write an answer using cout. DON'T FORGET THE "<< endl"
    // To debug: cerr << "Debug messages..." << endl;

    cout << "answer" << endl;
}