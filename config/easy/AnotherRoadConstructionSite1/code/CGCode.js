const roadLength = parseInt(readline()); // The total kilometers of the route.
const zoneQuantity = parseInt(readline()); // The number of road construction sites.
for (let i = 0; i < zoneQuantity; i++) {
    var inputs = readline().split(' ');
    const zoneKm = parseInt(inputs[0]); // The kilometer in which the road construction site begins.
    const zoneSpeed = parseInt(inputs[1]); // The speed limit of the road construction site.
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');


// Difference between theoretical time and travel time
console.log('0.00');