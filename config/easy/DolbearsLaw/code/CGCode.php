<?php
fscanf(STDIN, "%d", $M);
for ($i = 0; $i < $M; $i++)
{
    $LINE = stream_get_line(STDIN, 44 + 1, "\n");
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n
// To debug: error_log(var_export($var, true)); (equivalent to var_dump)

echo("answer\n");