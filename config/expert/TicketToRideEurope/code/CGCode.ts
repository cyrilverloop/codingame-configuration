var inputs: string[] = readline().split(' ');
const trainCars: number = parseInt(inputs[0]);
const numTickets: number = parseInt(inputs[1]);
const numRoutes: number = parseInt(inputs[2]);
var inputs: string[] = readline().split(' ');
const red: number = parseInt(inputs[0]);
const yellow: number = parseInt(inputs[1]);
const green: number = parseInt(inputs[2]);
const blue: number = parseInt(inputs[3]);
const white: number = parseInt(inputs[4]);
const black: number = parseInt(inputs[5]);
const orange: number = parseInt(inputs[6]);
const pink: number = parseInt(inputs[7]);
const engine: number = parseInt(inputs[8]);
for (let i = 0; i < numTickets; i++) {
    var inputs: string[] = readline().split(' ');
    const points: number = parseInt(inputs[0]);
    const cityA: string = inputs[1];
    const cityB: string = inputs[2];
}
for (let i = 0; i < numRoutes; i++) {
    var inputs: string[] = readline().split(' ');
    const length: number = parseInt(inputs[0]);
    const requiredEngines: number = parseInt(inputs[1]);
    const color: string = inputs[2];
    const cityA: string = inputs[3];
    const cityB: string = inputs[4];
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('points');