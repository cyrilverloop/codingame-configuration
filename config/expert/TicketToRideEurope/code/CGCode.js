var inputs = readline().split(' ');
const trainCars = parseInt(inputs[0]);
const numTickets = parseInt(inputs[1]);
const numRoutes = parseInt(inputs[2]);
var inputs = readline().split(' ');
const red = parseInt(inputs[0]);
const yellow = parseInt(inputs[1]);
const green = parseInt(inputs[2]);
const blue = parseInt(inputs[3]);
const white = parseInt(inputs[4]);
const black = parseInt(inputs[5]);
const orange = parseInt(inputs[6]);
const pink = parseInt(inputs[7]);
const engine = parseInt(inputs[8]);
for (let i = 0; i < numTickets; i++) {
    var inputs = readline().split(' ');
    const points = parseInt(inputs[0]);
    const cityA = inputs[1];
    const cityB = inputs[2];
}
for (let i = 0; i < numRoutes; i++) {
    var inputs = readline().split(' ');
    const length = parseInt(inputs[0]);
    const requiredEngines = parseInt(inputs[1]);
    const color = inputs[2];
    const cityA = inputs[3];
    const cityB = inputs[4];
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('points');