const n: number = parseInt(readline());
for (let i = 0; i < n; i++) {
    const permutation: string = readline();
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('order');