const N = parseInt(readline());
var inputs = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromNorth = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromWest = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromEast = parseInt(inputs[i]);
}
var inputs = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromSouth = parseInt(inputs[i]);
}
for (let i = 0; i < N; i++) {
    var inputs = readline().split(' ');
    for (let j = 0; j < N; j++) {
        const cell = parseInt(inputs[j]);
    }
}