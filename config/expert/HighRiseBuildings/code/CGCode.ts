const N: number = parseInt(readline());
var inputs: string[] = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromNorth: number = parseInt(inputs[i]);
}
var inputs: string[] = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromWest: number = parseInt(inputs[i]);
}
var inputs: string[] = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromEast: number = parseInt(inputs[i]);
}
var inputs: string[] = readline().split(' ');
for (let i = 0; i < N; i++) {
    const canSeeFromSouth: number = parseInt(inputs[i]);
}
for (let i = 0; i < N; i++) {
    var inputs: string[] = readline().split(' ');
    for (let j = 0; j < N; j++) {
        const cell: number = parseInt(inputs[j]);
    }
}