const surfacePointCount = parseInt(readline());
for (let i = 0; i < surfacePointCount; i++) {
    var inputs = readline().split(' ');
    const x = parseInt(inputs[0]);
    const y = parseInt(inputs[1]);
}
const xPortal = parseInt(readline());
const townCount = parseInt(readline());
for (let i = 0; i < townCount; i++) {
    var inputs = readline().split(' ');
    const xTown = parseInt(inputs[0]);
    const townName = inputs[1];
}

// Write an answer using console.log()
// To debug: console.error('Debug messages...');

console.log('lastFloodedTownName');